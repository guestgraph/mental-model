# Processes

One folder per process, written against `meta/core/process-schema.md`, with its phases in `phases/` against `meta/core/phase-schema.md` and its tracks in `tracks/` against `meta/core/track-schema.md`.
