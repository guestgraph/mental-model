# Surfaces

One file per surface, written against `meta/core/surface-schema.md` and its writing rules. A surface is a place anyone can reach without asking, so a place that is planned and not yet reachable has no file until it is. guestgraph.io is not one: its pages are written from the services and the talks, not from this model.
