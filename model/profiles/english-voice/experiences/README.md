# Experiences

One file per experience, written against `meta/core/experience-schema.md`. Empty: an agent owns no period until it has one worth recording. The folder is here because a profile owns experiences and R6 wants the folder beside the file.
